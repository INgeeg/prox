apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: kafkaexample-kafka-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/kafkaexample-kafka-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/kafkaexample-kafka-net: "true"
